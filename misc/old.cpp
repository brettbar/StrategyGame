
// Vector2 unitVector(Vector2 orig)
// {
//   float x = orig.x;
//   float y = orig.y;
//   float mag = std::pow(x*x + y*y, 0.5);
//   return { x / mag, y / mag };
// }


// float distance(Vector2 dest, Vector2 orig)
// {
//   return std::pow(std::pow(dest.x - orig.x, 2) + std::pow(dest.y - dest.y, 2), 0.5f);
// }
